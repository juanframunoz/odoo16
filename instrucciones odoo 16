bash 

# 1) Actualiza paquetes
sudo apt-get update
sudo apt-get upgrade -y

# 2) Instala Docker
sudo apt-get install -y docker.io

# (Opcional) Habilitar Docker para que inicie al arrancar
sudo systemctl enable docker
sudo systemctl start docker

# 3) Instala Docker Compose
sudo apt-get install -y docker-compose

sudo docker-compose up -d

Verificar contenedores en ejecución:

docker ps


cd /home
nano docker-compose.yml

version: '3'
services:
  db:
    image: postgres:15
    container_name: odoo16-db
    environment:
      - POSTGRES_USER=odoo
      - POSTGRES_PASSWORD=odoo
    volumes:
      - ./db-data:/var/lib/postgresql/data
    networks:
      - odoo-net

  web:
    image: odoo:16
    container_name: odoo16-web
    depends_on:
      - db
    ports:
      - "8069:8069"
    volumes:
      - ./odoo-extra-addons:/mnt/extra-addons:rw
    environment:
      - HOST=db
      - USER=odoo
      - PASSWORD=odoo
      - POSTGRES_DB=odoo
      - POSTGRES_USER=odoo
      - POSTGRES_PASSWORD=odoo
    networks:
      - odoo-net

networks:
  odoo-net:

  
  sudo docker-compose up -d

  docker ps
  
  Si no funciona:
  
  docker-compose down -v
docker-compose up -d


Si te sale error 500 inmediatamente, forzar la instalación “base”:
bash
Copiar código
docker-compose exec web \
    odoo -d odoo --db_host=db --db_user=odoo --db_password=odoo \
    -i base --stop-after-init
